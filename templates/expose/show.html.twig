{% extends 'base.html.twig' %}

{% block title %}Expose{% endblock %}

{% block body %}
    <h1>Expose</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ expose.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ expose.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ expose.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('expose_index') }}">back to list</a>

    <a href="{{ path('expose_edit', {'id': expose.id}) }}">edit</a>

    {{ include('expose/_delete_form.html.twig') }}
{% endblock %}
